import {motion} from "framer-motion"

const ToggleButton = () => {
  return (
    // <button onClick={()=>setOpen((prev) => !prev)}>
        <div></div>
    // </button> 
  )
}

export default ToggleButton;